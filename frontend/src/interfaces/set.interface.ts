export interface Set {
  slug_set: string;
  slug_match: string;
  set_number: number;
  local_points: number;
  visitor_points: number;
  finished_at: Date;
  status: string;
  is_active: boolean;
}
